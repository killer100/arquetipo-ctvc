// const root = document.getElementById("root");

// const config = JSON.parse(root.getAttribute("data-app"));

// root.removeAttribute("data-app");

const config = {"urlBaseProject":"/ctvc","urlFileServer":"https://almacenpncm.blob.core.windows.net/sispad/documents/","userName":"","userFullName":null};

export const AppConfig = config;
